use_frameworks!

workspace '../FlexLayout.xcworkspace'

target 'PinLayoutSample' do
  project 'PinLayoutSample.xcodeproj'

  #pod 'FlexLayoutTest'
  pod 'FlexLayoutTest', :path => '../'
  pod 'PinLayout'

  # Debug only
  pod 'Reveal-SDK', :configurations => ['Debug']
  pod 'SwiftLint'
end
